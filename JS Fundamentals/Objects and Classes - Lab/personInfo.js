function personInfo(first, last, age) {
    let personInfo = {
        firstName: first,
        lastName: last,
        age: age
    }
    return personInfo;
}
console.log(personInfo("Peter", "Pan", "20"));